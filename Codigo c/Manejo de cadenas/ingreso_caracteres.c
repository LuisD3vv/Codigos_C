#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(){

    char nombre[11]; //n-1

    for (int i = 0; i < 11; i++){
        printf("ingresa un caracter %d: ",i);
        scanf("%c ", &nombre[i]);
    }
    printf("%s", nombre);
    
    for (size_t i = 0; i < count; i++)
    {
        /* code */
    }
    
}