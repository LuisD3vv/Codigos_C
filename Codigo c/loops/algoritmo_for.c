/*#include <stdio.h>

int main(){
//la sintaxis de for se compone basicamente de 4 elementos, inicializador, condicion, incremento y dentro del esto, el codigo a ejecutar

for(int = 50; i<=150; i++) {
      printf("%d El numero es :", i);
  }

return 0;
}
*/
#include <stdio.h>

int main(){

int contador = 49;

for (int i=0; i<101; i++) {
contador = contador + 1;
    printf("%d\n El numero es: ",contador + 1);
}
return 0;
}
/*
ejemplo

for(inicializador; condicion ; incremento)
    //bloque a ejecutar si la condiciones true

hay que tomar en cuenta los formatos de salida siempre, ya que son muy importantes para todo.
*/
