#include <stdio.h>

void main (){
    int numero =10;
    contador = 0;

    while (contador < numero)
    {
        if (contador==5)
        continue;
        printf("contador %d", contador);
    }


}