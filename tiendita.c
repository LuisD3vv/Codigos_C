#include <stdio.h>
int main(){
int coca = 15, jugo = 10, galletas = 13, resultado = 0, cantidad_cocas = 0, cantidad_jugos = 0, cantidad_galletas = 0;
float descuento = 0.15, descuento_total = 0;

//imprimir variables al usuario

	printf("Bienvenido, aqui esta nuestro menu\n, Galletas");
		printf("%d Galletas\n", + galletas);
		 scanf("%d cantidad de galletas", &cantidad_galletas);
		printf("%d Coca\n", + coca);
		 scanf("%d cantidad de cocas", &cantidad_cocas);
		printf("%d Jugo\n", + jugo);
		 scanf("%d cantidad de jugos", &cantidad_jugos);


// solicitar datos al usuario
resultado = cantidad_cocas * cocas + cantidad_jugos * jugos + cantidad_galletas * galletas;
//verificar si se aplica el descuento o no
	if(resultado >= 300)

// imprimir en caso si y en caso no
return 0;
} 
